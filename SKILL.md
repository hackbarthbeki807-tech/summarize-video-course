---
name: summarize-video-course
description: Use when本地视频课程、字幕或逐字稿需要整理、批量处理、跨回合持续监控或交付；尤其适用于任务运行时间长、状态容易被误解、只收到部分资料、文件难找、手机打不开、技术报告难懂，或继续下一批前需要人工确认的场景。
---

# 视频课程总结与交付 v2.8

## 核心标准

把课程来源整理成可读、可搜索、可追溯的文字知识库，并把长任务持续推进到约定终点。只有当用户能找到、能打开、能看懂完整课程资料，并且清楚任务是否仍在运行、下一步要做什么时，才算完成交付；程序启动、对话暂时结束、报告生成或文件夹存在都不等于任务完成。

## v2.8：用户反复手动修正吸收清单

这一版把用户在多次交付后反复补改的动作，变成不可跳过的生成、验证和交付规则：

| 用户反复做的调整 | v2.8 固定规则 | 完成前验证 |
|---|---|---|
| 反复问“我看什么”，把文件夹、`.cmd` 或批次报告换成真正文章 | 交付必须直接给实际可读的 Markdown；技术入口和处理报告不能冒充课程内容 | 打开将发送的同一个最终链接，确认文件非空、是正文而非目录/脚本 |
| 反复纠正“这节课讲什么看不出来” | 总览和逻辑稿先写 3–8 条完整“本课学会什么”，再写问题、结论、顺序和适用对象 | 逐项检查栏目有主语、动作、对象，不能是时间戳残句 |
| 反复把“老师原话”和模型整理分开 | 原话必须能逐字回到逐字稿；归纳、意译和待核对内容分别标注 | 抽查来源时间戳和内容性质；不确定不得加引号 |
| 反复补知识点、公式、类型、适用场景和例子 | 先判断框架方法型、课堂推导型、清单工具型或混合型；一张卡只讲一个主题，不设固定数量 | 检查每个知识点可独立复述，空字段不编造 |
| 反复要求案例不是摘抄，而是能看懂的完整事情 | 有情境时按“背景—问题与分析—动作—结果—启发”整理；材料完整时升级为案例复盘型八段结构 | 读者不看视频也能说清发生了什么、为什么改、怎么做、结果和限制 |
| 反复补案例的背景、分析、动作、结果和限制 | 背景必须写主体/产品、原状态和触发问题；数据、调整、失败、不可持续性没有来源就明确写“来源未说明” | 检查没有“课堂以……为例”等抽象占位，也没有模型编造的指标 |
| 反复发现任务停止却不知道是否完成 | 长任务先进入目标模式，持续监控并主动通知；启动、断点、对话结束都只算处理中 | 记录运行标识、最后检查时间、下一动作；真正完成后才发完成通知 |
| 反复在批次验收前拿不到文件，或文件不适配设备 | 验收消息必须同条附本批新生成且现场打开的 `01-逻辑稿.md`，同时列出其余六项真实文件；手机不能只给 D 盘路径 | 用本批 manifest、`media_index.json` 和最终发送链接三重核对；缺文件就不得发起验收 |
| 反复担心改坏原成果或误碰源视频 | 先 staging、逐文件验证、再发布并保留备份；源视频只读，输出不含音视频和绝对源路径 | 检查哈希、大小、修改时间、七项结构、断链和媒体文件数 |

这张表是本 Skill 的执行清单，不是建议项；任一项未通过，状态只能写“处理中/待补充/尚未到验收阶段”。

## 什么场景下用它

- 把本地课程视频、字幕或逐字稿整理为逻辑稿、总览、卡片等知识资料。
- 制作 3–5 个视频的小样、模型对比、固定批次试跑或整门课程成果。
- 将文字成果放入 Obsidian 或其他本地知识库。
- 向非技术用户交付结果，尤其是用户问“我看什么”“这个是干什么的”“下一步做什么”时。
- 修复路径错误、阅读入口不清楚、把 `.cmd` 当阅读文件、电脑文件发给手机等交付问题。

纯媒体扫描可以使用专门扫描器；一旦涉及课程内容整理、阅读体验或人工验收，就使用本 Skill。

## 用它要产出什么

### 每课文字成果

按任务粒度将第一项命名为 `00-本课总览.md` 或 `00-课程总览.md`，其余名称固定：

```text
00-本课总览.md / 00-课程总览.md
01-逻辑稿.md
02-时间戳逐字稿.md
03-知识点卡片.md
04-案例与金句.md
05-质量问题清单.md
media_index.json
```

- 总览：第一节必须写“本课学会什么”，列出 3–8 条明确知识点，再用普通话说明讲了什么、为什么重要、核心结论、建议阅读顺序。
- 逻辑稿：第一节必须写“本课学会什么”，列出 3–8 条明确知识点；先判断课程属于“框架方法型、课堂推导型、清单工具型或混合型”，再按最贴合原课的结构重组；保留可核对时间戳。
- 时间戳逐字稿：保存原始来源记录，不把它当成日常阅读稿。
- 知识点卡片：每张卡片只表达一个可独立复用的概念、方法或判断；数量按逐字稿真实知识点决定，不设固定上限；明确区分“老师原话”和“模型整理”。
- 案例与金句：只收录原课中真实存在且能定位的案例、原话或明确标注的意译。
- 质量问题清单：记录漏写、重复、低置信度、专有名词、时间戳、硬字幕等待人工核对事项；不静默改写。
- 来源索引：只保存必要的相对路径、时长、校验值、对应文字文件及本地处理标记，不泄露绝对源路径。

### 知识成果发布闸门（v2.2）

- “本课学会什么”是正式成果的必要内容，不是装饰标题。它必须出现在 `00-本课总览.md` 和 `01-逻辑稿.md` 的最前面，并有 3–8 条能脱离视频理解的知识点。
- 知识点只能来自本课逐字稿；每条应带来源时间戳，并在 `03-知识点卡片.md` 中保留“老师原话 / 模型整理 / 重点核对”边界。
- 逐字稿摘录、时间戳抽样和“待核对”占位内容只能放在处理记录或失败断点，不能发布成正式课时的总览、逻辑稿或知识卡片。
- 本地模型结构化输出失败、知识点少于 3 条、或总览/逻辑稿只有摘录时，课时状态必须停在“知识整理未完成”；保留转写断点，等待重新整理，不得把安全回退版标记为完成。
- 已通过人工验收的旧课时不自动覆盖；若用户要求修复，先生成可恢复备份并说明修复范围。

### 笔记类型选择与 AI 可读结构（v2.8）

- **框架方法型**：课程大量讲分类、模型、公式、方法、适用场景和案例时使用；逻辑稿采用“定义/问题 → 分类或模型 → 方法/公式 → 场景 → 案例”。
- **课堂推导型**：课程按“提出问题 → 解释概念 → 推导判断 → 案例/问答 → 练习”推进时使用；保留老师的讲授顺序，不强行重排成统一模板。
- **清单工具型**：课程大量讲类型、模板、步骤、标题、方法和例子时使用；每一类单独列出适用范围、写法、公式、特点和例子。
- **混合型**：信号接近或课程同时包含推导和清单时，外层保留授课顺序，内层拆成知识点块。
- 自动判断只能作为路由提示；复杂课时可以用课程状态目录中的 `note_style_overrides.json` 人工覆盖。
- `01-逻辑稿.md` 的每个 `###` 知识点块应尽量使用稳定字段：所属主题、核心定义、解决问题、适用场景、方法或公式、主要特点、具体例子、内容性质、来源时间戳。没有来源的字段不补写。
- `03-知识点卡片.md` 是 AI 检索层；一张卡只表达一个主题，公式、例子和来源分开写。人类阅读不应被字段说明淹没，但字段名称必须稳定。
- 课件图片或图表没有可读取文字时，只标注“课件图示未提取”，不得根据 `[图片]` 占位符猜测。
- 已有课时结构升级必须先进入 `work/note-upgrade-staging-*/`，样稿人工确认前不覆盖正式 `outputs/`，也不启动下一批视频。
- 本地模型长时间不可用时，可使用 `course_note_upgrade.py --reuse-existing` 迁移已有完整正式文字成果；这不是逐字稿占位回退，仍需七项结构、哈希、隐私和人工可读性检查，且不得直接覆盖正式目录。

### 用户反复手动修正吸收规则（v2.8 强制执行）

本版本把用户在多次生成后反复手动做的修正，改成生成器和交付者都必须执行的固定闸门。不能再把这些问题留到用户阅读后才发现。

#### 生成前：先确定“这节课要怎么记”

1. 先读取课程状态、已批准范例和本课逐字稿，再选择框架方法型、课堂推导型、清单工具型或混合型；不得默认所有课都套“背景—框架—步骤—注意事项”。
2. 每课先建立一个明确的知识主线：老师按什么顺序讲、每一段解决什么问题、哪些内容是定义/方法/公式/案例/练习。外层保留授课顺序，内层拆成可独立阅读的知识点。
3. 生成前扫描“比如、例如、举个例子、案例、假设、真实产品、用户场景”等提示及前后文；案例页不能因为模型没抓到就直接写“本课未发现”。
4. 图片或课件图表不可读时只写“课件图示未提取”，不得根据 `[图片]` 或空占位符猜测事实；如果该图可能承载核心公式、流程、结论或案例，必须在 `05-质量问题清单.md` 标为“关键课件图示缺失”，课时状态停在“知识整理未完成”，不得发布正式课时。

#### 阅读层：把处理结果改成真正的课程笔记

1. `00-本课总览.md` 和 `01-逻辑稿.md` 开头都必须有“本课学会什么”，写 3–8 条脱离视频也能理解的知识点；不能用时间戳摘录充数。
2. “本课解决的问题”必须回答一个完整问题；“核心结论”必须是完整判断句；“实际操作顺序”必须是带动词、可执行的步骤；“适合谁看”必须如实说明对象。禁止把多个残句拼成栏目内容。
3. 日常阅读稿必须能让用户只看文件就知道“这节课讲什么、学会什么、怎么用”；批次统计、模型对比和运行日志只能放处理记录，不能冒充课程正文。
4. 每个知识点只表达一个主题。卡片使用稳定字段，按真实内容决定数量；没有依据的字段留空或明确写“本课未发现”，不为凑格式补写。
5. 所有内容都标明性质：能在逐字稿逐字找到的短句才标“老师原话”，模型归纳写“模型整理”，不确定写“意译/待人工核对”；来源时间戳只是定位，不是原话证明。
6. 发现真实案例时，案例必须同时进入 `01-逻辑稿.md` 对应知识点和 `04-案例与金句.md`；没有原句依据时标为“模型提炼”或“意译”，不能伪造引号。转写不完整的“意译（待人工核对）”只能进入 staging 或质量问题清单，核对前不得发布正式课时或发起批次验收。

#### `04-案例与金句.md` 的案例最低结构（v2.8）

“案例”不是一句知识点摘要。每个案例至少按下面顺序写成一个可复述的小故事：

```markdown
### 案例标题：一句话说清发生了什么

- 案例类型：真实案例 / 行业案例 / 课堂示例 / 正反例 / 假设案例
- 背景：必须让读者一眼知道主体/产品、原来的做法或现状、以及触发问题；不能只写“课堂以……为例”“老师指出……”这类方法说明
- 问题与分析：具体遇到什么问题，为什么原做法无效或需要改变
- 动作/改造方式：采取了什么动作、流程或方案
- 结果：产生了什么结果；来源没有说明时写“来源未说明”，不得猜测
- 可复用启发：对读者有什么可迁移的判断或做法
- 来源：时间戳或外部链接
- 数据可信度：老师讲述 / 案例发布者自述 / 第三方材料 / 未核实
- 内容性质：老师原话 / 模型整理 / 意译 / 外部资料整理
```

- `背景 → 问题与分析 → 动作/改造方式 → 结果 → 可复用启发` 是最低可读链条；没有这条链条的内容只能放在“知识点示例”或“待补充案例”，不能冒充完整案例。
- 背景、问题与分析、动作、结果和启发必须由模型根据逐字稿整理成完整句；逐字稿原始片段只能放在“来源摘录/证据”字段，不能直接填入案例正文。案例页要让用户不看视频也能复述“发生了什么、为什么、怎么做、得到什么”。
- 课堂中的具体正例、反例或假设情境也可以收录，但必须标为“课堂示例/正反例/假设案例”，不能写成真实项目结果。
- 外部行业案例可以采用同一结构，但必须保留外部来源、数据可信度和“未找到第三方审计”等限制说明；不得把外部案例的数据写成课程老师讲过的事实。
- “支付阶段用户流失”“北京地区女性用户”等只有在补齐背景、问题、动作和结果后，才可作为案例；否则应标为“课堂示例”。

#### 有完整材料时：使用“案例复盘型”扩展结构（参考用户提供的运营案例）

当来源材料同时包含项目背景、目标、执行过程、数据、调整或失败记录时，案例页扩展为：

```text
案例标题（时间/平台/动作/结果，结果没有来源时不写结果）
一、项目背景
二、目标与初始问题
三、执行动作
四、数据结果
五、关键转折点与调整
六、失败、限制与未验证之处
七、可复用经验
```

- 标题优先包含时间、平台/产品、动作和结果；缺少任何一项就不强行补齐。
- 数据结果必须列出原文明确给出的数量、比例、时间和口径，并标明来源可信度；没有数据时写“来源未说明”。
- “发现问题 → 做出调整 → 数据或行为变化”是案例的高价值部分；来源没有后续变化时，明确写“逐字稿未说明调整后的变化”。
- 失败、限流、成本、不可持续性和偶然性不能省略；材料没有提到时写“来源未说明”，不能凭经验补写。
- 运营案例中的具体做法只作为结构参考；过时的平台规则、非原创内容或未经核实的外部数据不能直接迁移为建议。
- 课堂短示例没有足够数据时，继续使用五段最低结构，不为了套用八段复盘模板制造“数据结果”。

#### 金句的最低标准（v2.8）

- 金句必须是能脱离上下文使用的完整表达，优先保留老师原句；不能把模型总结句、关键词或半句话冒充金句。
- 每条金句必须有来源时间戳，并标明“老师原话 / 原话候选（待音频核对）/ 模型提炼”。无法逐字确认时不得加引号。
- 金句与“可复用表达”分开：金句强调原课表达，可复用表达强调读者可以迁移使用的句式；二者都没有时才写“本课未发现”，且必须在质量清单记录扫描范围。

#### 交付前：先把用户真正能看的文件准备好

1. 每课必须同时生成七项资料：总览、逻辑稿、时间戳逐字稿、知识点卡片、案例与金句、质量问题清单、`media_index.json`。只给逻辑稿、文件夹、`.cmd`、批次报告或状态文件都不算交付。
2. 用户问“我看什么”时，直接给一个实际存在、非空、已现场打开验证的可阅读文件；不得让用户自己从文件夹或运行目录里找，也不得用计划中的路径替代实物。
3. 发起验收的同一条消息中，必须给出本批新生成课时的一篇真实 `01-逻辑稿.md` 作为“请先看”入口，并逐项列出该课其余六项真实文件和用途；缺文件就只能说“尚未到验收阶段”。
4. 交付路径必须适配用户设备。电脑端可给已验证的本地文件链接；手机端不能只给 `D:\...`，也不能因为路径含 `BaiduSyncdisk` 就假定手机可访问。未经授权不得上传或复制课程内容。

#### 批次状态：把“生成完了”与“用户看到了”分开

1. 后台启动、进程存在、断点存在、部分课时完成、报告生成，都只能写“处理中/可恢复”，不能写“批次完成”。
2. 长任务必须先进入目标模式并持续监控；完成时主动通知，不能让用户靠追问发现结果。每批完成后停在人工验收闸门，未经明确通过不启动下一批。
3. 任何模型失败、结构不完整、只有逐字稿摘录或案例无法核对的课时，都停止该课并标记“知识整理未完成”；不能发布看起来完整的占位版。
4. 已验收课时不自动覆盖；升级先写入 `work/note-upgrade-staging-*`，验证通过后再发布并保留可恢复备份。

#### 阅读栏目必须“成句、成义、能直接使用”

`00-本课总览.md` 与 `01-逻辑稿.md` 里的阅读栏目不是逐字稿检索结果，必须让用户只看这一页就知道这节课讲了什么：

- **本课解决的问题**：用 1 段 2–4 句完整陈述，直接回答“这节课解决了什么问题”。句子必须有主语、动作和对象，不能把“解决这个问题”“跟传统的这种视频”之类残句拼接出来。
- **核心结论**：列出 2–5 条完整判断句，每条都能独立表达一个结论，而不是关键词、半句话或时间戳摘录。
- **实际操作顺序**：列出 2–6 步，每步以明确动词开头，能按顺序执行；不能只列“分析、设计、监控”这类名词。
- **适合谁看**：用 1–2 句如实说明适用对象；逐字稿没有明确说明时，必须写“逐字稿未明确说明适用对象”，不能猜测或套模板。
- 上述四栏都禁止出现“逐字稿摘录（待核对）”、孤立时间戳或无法独立理解的残句。任一栏目不满足时，课时必须标为“知识整理未完成”，不能进入正式阅读区，也不能进入批次验收。

### 知识点卡片标注与人工核对分级（v2.2）

这是降低人工负担的规则，不是把逐条校对任务转给用户。

- **老师原话**：只有能在时间戳逐字稿中逐字找到、并带来源时间戳的短句，才允许标为“老师原话”。无法确认时必须写“未单独提取”，不能为了完整而编造引号内容。
- **模型整理**：知识点、解释、适用场景、操作提示和模型改写默认归入“模型整理”，不能伪装成老师的原句或讲师观点。
- **人工核对级别**：含引号、数字、专有名词、公式、案例、强结论或低置信度内容时标为“重点核对”；其余标为“常规阅读”。标签由工具自动生成，用户不需要逐条核对全文。
- **用户默认动作**：先正常阅读总览、逻辑稿和卡片；只核对“重点核对”项、明确的“老师原话”，以及用户自己觉得可疑的内容。批次验收时抽查一篇逻辑稿的开头、中间、结尾即可，不要求逐课逐卡全文比对。
- **案例与金句**：每条必须注明“老师原话 / 意译 / 模型提炼 / 本课未发现”。没有原句依据的内容只能写意译或模型提炼。
- **案例识别双检**：写“本课未发现”前，必须扫描逐字稿中的“比如、例如、举个例子、案例、假设、小明/小白/小黑、真实产品或用户场景”等提示及其前后文。只要存在可定位的具体情境，就应输出带时间戳的“模型提炼”或“意译”；提示存在但转写不完整时，写“意译（待人工核对）”，不能把真实案例漏写成“本课未发现”。
- **来源时间戳不是原话证明**：时间戳只说明回到逐字稿的位置；是否为原话仍由“老师原话”标签和逐字稿内容共同确认。

### 课程化归档

遵守“一门课程一个根文件夹”。课程根目录唯一入口是 `00-课程总览.md`；处理阶段目录不得平铺在 `outputs/` 根目录。

```text
outputs/
└── <课程名>/
    ├── 00-课程总览.md
    ├── 课程级索引与 media_index.json
    ├── 课时资料/
    │   └── <课时名>/
    │       └── 七项正式成果
    └── 99-处理记录/
        └── 扫描、试验、批次和历史记录
```

- 正式成果进入 `课时资料/`；处理记录进入 `99-处理记录/`。
- 时间戳运行目录、模型试验、批次报告和启动程序不得与正式课时并列成多个课程入口。
- 课程总览、索引和链接必须指向唯一权威版本；历史副本只留在处理记录中。

### 批次辅助报告

可以生成耗时、质量标记和完成状态汇总，但它只是处理报告，不能替代课程总览、逻辑稿和知识点卡片。不得让用户阅读批次汇总来判断课程内容质量。

### 最终交付说明

每次交付必须直接回答：

```text
现在：做到哪一步。
请先看：一个真实存在、可阅读的具体文件，以及为什么先看它。
完整资料：明确说明本课七项均已生成，并逐项给出真实文件入口与用途。
你要做：最多 1–3 个普通话动作。
请回复：用户可直接复制的一句话，例如“通过，可以继续”或具体问题。
未确认前：明确不会处理哪些视频、不会删除什么、不会扩大什么范围。
设备说明：这个入口适用于电脑还是手机；若不适用，直接说明。
```

## 长任务目标模式硬闸门（开始运行前）

**铁律：会跨回合、需要等待或监控的本地批次，先创建持久目标，再启动本地处理。不能先启动任务，再补建目标。**

满足任一条件即视为长任务：预计超过一次正常回复能够完成；预计运行超过 60 秒；工具返回后台运行标识；需要轮询、断点续跑、模型服务或多个课时；用户要求“持续运行”“完成后通知”或同等结果。

### 环境支持目标模式

1. 启动前检查是否已有同一任务的活动目标；有则继续该目标，不重复创建，不覆盖无关目标。若当前有另一个未完成目标，不能复用、覆盖、擅自完成或绕开它，必须说明：“当前有另一个未完成目标，本批尚未启动。请先处理现有目标，或在支持独立目标的新任务环境中继续。”
2. 没有活动目标时，自动创建持久目标。目标必须写明具体批次、完成条件、验证条件、主动交付动作以及“未经人工验收不开始下一批”。
3. 目标创建成功后才能启动转写、知识整理或批处理。启动消息必须明确说“目标模式已开启”“当前仍在处理中”和预计时间。
4. 持续监控运行状态、断点和日志；需要等待时使用可恢复的等待机制，每次等待不超过 60 秒，并记录运行标识、最后检查时间和下一检查动作。不得静默停止监控，也不得把下一次询问责任推给用户。不能只凭“目标模式会自动续接”结束实际监控。
5. 只有批次进程结束、预期课时全部发布、本批每一个课时的七项结构完整且六个 Markdown 均逐项打开、每个 `media_index.json` 均成功解析、后台验证完成、交付文件可打开且完成消息已发送，才可以把目标标记为完成。

### 当前环境没有目标模式

必须在启动前明确告诉用户“当前环境没有目标模式”，不得静默降级为普通单轮任务。给出下面这段可直接复制的指令，请用户启用目标模式后发送；未确认启用前不启动长批次：

```text
请把当前任务切换为目标模式：持续运行并监控本批任务，直到本批全部课时真正生成并完成验证；完成后主动回复我，并附上当前设备能直接打开的实际课程文件。中途状态更新不代表完成，未经我验收不要开始下一批。
```

如果平台确实不提供目标模式，说明限制和中断风险，再由用户决定是否改用可恢复的分段执行；不能声称普通后台启动等同于持续目标。单个分段最多处理一个课时，并且必须全程使用不超过 60 秒的等待持续监控，直到该段完成或明确失败；每段结束都保存断点并明确状态。**分段执行不得换名后台长跑**。

### 运行状态防误解

**启动成功不等于任务完成。** 对话轮次结束、后台进程已启动、工具返回运行 ID、断点文件存在、部分课时完成，都只能证明任务已开始或可恢复。

处理中只使用下面这种状态表达：

```text
现在：第 N 批仍在处理中，尚未到验收阶段。
当前：已完成 X/Y，正在处理第 Z 课；这不是完成通知。
下一步由我：继续监控、验证并生成完整交付。
你现在不用操作。完成后主动回复，并附上实际课程文件。
```

若任务意外中断，必须说明“已中断/已暂停”，记录断点并在授权范围内恢复；不能发出像阶段总结一样的完成式措辞。任何状态消息都要明确区分“正在运行”“暂停/失败”“批次完成”。

## 批次验收文件硬闸门（不可跳过）

**铁律：没有本批真实、已验证、用户当前设备能打开的课程文件，就不准发起验收。**

### 完整七项交付硬闸门

**生成完整不等于说明完整。不能只发一篇逻辑稿，让用户猜测逐字稿或知识点是否存在。** 每完成一批，必须先完成下面八步，顺序不能颠倒：

1. 确认批次状态是完成，而不是运行中、失败或只有断点。
2. 从本批新发布的正式课时中选一篇 `课时资料/<课时名>/01-逻辑稿.md`；用本批 manifest 中的课时 ID、相对源路径和该课 `media_index.json` 交叉确认它属于本批，不能选旧批次文件。
3. 验证本批每一个课时的七项结构完整：所有课时的六个 Markdown 均逐项打开并确认非空、名称正确，所有 `media_index.json` 均成功解析。选中交付课还要核对内容可读性和来源。空内容必须如实标注，例如“本课未发现案例”，不能用占位文件冒充丰富内容。
4. 现场打开首要阅读文件，确认内容可读且不是 `.cmd`、文件夹、批次报告、状态文件或临时稿；生成最终交付链接后，还必须用**将要发给用户的同一个链接**重新打开验证一次。
5. 用实际证据确认用户当前设备能打开整套资料，并确认七个入口均适用于用户当前设备。电脑端逐项验证最终文件链接；手机端逐项验证用户授权传递后的实际附件或页面。不能仅凭首篇可打开、目录名、同步软件名称或主观推测判定整套资料“能打开”。
6. 在**同一条消息中说明“本课完整资料共七项”**，逐项给出七个真实文件入口：总览、逻辑稿、时间戳逐字稿、知识点卡片、案例与金句、质量问题清单、`media_index.json`。不能只发一篇逻辑稿。
7. 同一条消息只设一个“请先看”的优先入口，推荐顺序是“总览 → 逻辑稿 → 知识点卡片”；同时说明逐字稿用于核对原话，质量清单用于人工复核，`media_index.json` 用于来源追溯。一个优先入口不等于隐藏其他六项。
8. 完成整套文件交付后，才可以要求用户回复“本批通过，可以继续下一批”或指出问题。

### 发送前强制自检

只要准备发送的消息包含“验收”“通过”“批准下一批”“可以继续”中的任何一个意思，发送前必须同时满足：

```text
[ ] 本批已经完成
[ ] 本批至少一篇新的正式 01-逻辑稿.md 已发布
[ ] 我刚刚实际打开并读过该文件
[ ] 文件真实存在且非空
[ ] 本批 manifest 与该课 media_index.json 能证明文件属于刚完成的这一批，不是旧样本
[ ] 我已用将要发送的同一个最终链接重新打开验证
[ ] 该课七项成果均已打开或解析并确认真实、非空、名称准确
[ ] 同一条消息里有一个明确“请先看”入口，并逐项给出完整七项文件入口
[ ] 我已说明七项用途和默认阅读顺序，没有让用户误以为只有逻辑稿
[ ] 我有实际证据确认该入口适用于用户当前设备
```

任何一项不满足：**删除验收请求**，只汇报“仍在处理中/尚未到验收阶段”。

### 不允许的替代做法

| 说法或做法 | 为什么不算交付 |
|---|---|
| “你先验收，文件稍后给” | 用户没有内容可看 |
| 只说“完成后你看逻辑稿” | 这是未来说明，不是本次交付 |
| 给课程文件夹或批次文件夹 | 用户仍要自己寻找 |
| 给 `.cmd`、manifest、运行日志或批次汇总 | 这些不是课程阅读材料 |
| 给一篇旧批次逻辑稿 | 不能验收刚完成的新批次 |
| 只给一篇新逻辑稿，不说明其余六项 | 用户会合理地以为只生成了逻辑稿 |
| 手机端只给 `D:\...` 本地路径 | 当前设备打不开 |
| 先讲验收标准，但不给文件 | 仍然是在发起无文件验收 |

### 合格的批次验收消息

```text
现在：第 N 批已完成并通过后台验证。
请先看：[本批某课：01-逻辑稿.md](真实、已验证、当前设备可打开的文件入口)
为什么看它：一句普通话说明选这篇的理由。
本课完整资料共七项：
1. [00-本课总览.md](真实入口)——先了解本课讲什么
2. [01-逻辑稿.md](真实入口)——日常阅读正文
3. [02-时间戳逐字稿.md](真实入口)——核对原话
4. [03-知识点卡片.md](真实入口)——复习与复用
5. [04-案例与金句.md](真实入口)——原课案例和可定位表达
6. [05-质量问题清单.md](真实入口)——待人工核对事项
7. [media_index.json](真实入口)——来源追溯
你要做：读这一篇，判断能否脱离视频看懂，有没有明显胡说、漏重点或重复。
请回复：“第 N 批通过，可以处理第 N+1 批”，或直接指出问题。
未确认前：不会开始下一批。
```

这一步是批次闸门本身，不是可选的交付礼貌。**缺少文件的验收消息视为未交付；只交一篇的验收消息也视为未交付，均不能据此获得下一批授权。**

## 必须遵守的规矩

### 先把事情说清楚

1. 开工前说明当前阶段、已完成范围、这一步由谁做、预计时间、磁盘和可能的额度/费用。区分本地计算、模型下载和对话平台额度，不含糊地说“免费”或“没额度”。
2. 技术步骤默认由助手完成。不要把安装、命令、路径判断或模型指标甩给用户；只有内容抽查、授权和主观选择需要用户处理。
3. 需要用户决策时先给明确建议，再说明理由，最后只要求一个简单回复。不要要求用户自己理解 RTF、置信度、token、字数等技术指标后做决定。
4. 每个阶段设置人工闸门。只把用户的批准解释为当时明确约定的范围；未批准的剩余视频不得自动处理。
5. 长任务在运行前逐项通过“长任务目标模式硬闸门”。目标模式、后台进程和断点状态各自解决不同问题，不能相互冒充。

### 交付实际课程内容

6. 默认阅读顺序为：总览 → 逻辑稿 → 知识点卡片；逐字稿和质量清单只在核对时打开。
7. 用户问“我看什么”时，直接给一篇具体的 `.md`、`.html`、`.pdf` 或其他可阅读文件。不得给 `.cmd`、程序、文件夹或批次运行入口冒充阅读材料。
8. `.cmd` 只能标注为“电脑端启动/打开文件夹的工具”，不能称为总结、报告或文章。
9. 发出路径前必须现场验证文件真实存在、非空、名称准确、内容可读。不得根据计划或预期结构拼接一个未经验证的路径。
10. 只设一个明确的“请先看”优先入口，但同一条交付消息仍须列出完整七项资料。不要让用户在多个时间戳目录里寻找最终版本。
11. 报告成功、测试通过和文件数量正确只是后台验收。用户找不到、打不开、看不懂或不知道下一步时，交付仍然失败。
12. 批次验收必须逐项通过“批次验收文件硬闸门”和“完整七项交付硬闸门”。处理中、只有断点文件或尚未产生新逻辑稿时，只能明确汇报“尚未到验收阶段”，不得提前讲验收步骤造成用户无文件可看。

### 适配用户设备

13. 交付前确认或依据用户刚刚提供的信息判断其使用电脑还是手机。本地 Windows 路径和 `.cmd` 只适用于存放文件的电脑，不能当作手机阅读链接。
14. 用户在手机端时，直接说明本地文件无法由手机打开，并询问其授权的传递方式。不得擅自上传课程内容，也不得把课程全文粘贴到远程对话中。
15. 路径中出现 `BaiduSyncdisk`、OneDrive 等名称，只代表本地目录名称，不代表文件已同步、手机能访问或用户授权使用该云服务。

### 保持本地、只读和可追溯

16. 默认全程本地处理，不把视频、音频、字幕、逐字稿、整理稿或提示词发送给远程模型、付费 API 或云端服务。
17. 源视频保持只读，不复制、移动、重命名或删除。临时媒体只能进入项目规定的 `work/`，最终输出不得保留音视频或转码文件。
18. 正式处理时保留来源相对路径、时长、SHA-256 和时间戳，验证源文件大小、修改时间或校验值未变化。
19. 成品只发布到稳定、清楚的最终阅读目录；中断稿、临时稿和模型原始输出不得冒充完整成品，重复运行不得覆盖已经批准的成果。

### 以普通话交付

20. 先讲结论和用户动作，再讲技术细节。把“RTF 1.06”翻译为“处理 10 分钟视频约需 11 分钟”；技术指标可放在后面供追溯。
21. 明确区分“处理报告”和“课程内容”。用户日常阅读的是总览、逻辑稿和卡片，不是模型对比、批次统计或质量指标。
22. 知识点卡片的标签和重点核对级别由工具自动生成；不得要求用户逐条核对，也不得把“需要抽查”说成“每篇都要人工验收”。
23. 遇到用户已纠正过的交付问题，先修正固定规则和项目状态，再继续下一步，避免换阶段后重复犯错。

## 执行流程

1. 读取项目规则、状态和实际文件，确认已完成范围、未批准范围与用户当前设备。
2. 小样优先。批量前先估算时长、磁盘、联网下载和费用/额度，并取得明确批准。
3. 判断是否属于长任务；属于时先进入目标模式并写明终点，再启动处理。没有目标模式时先给用户启用指令，不能静默降级。
4. 持续运行和监控；中途只发明确的运行状态，不把启动、断点或一次对话结束说成完成。
5. 从字幕或逐字稿提取事实，生成每课七项文字成果；不要依赖范例补写新课程内容。
6. 验证时间戳单调且未超视频时长、文件非空、结构完整、来源可追溯、输出无音视频和绝对源路径；同时确认卡片含来源标注和人工核对级别。
7. 现场验证最终阅读文件和路径，逐项通过批次验收与完整七项交付硬闸门；同一条验收消息设置一个优先入口并列出七项实际文件。
8. 完成主动交付后停在约定的人工闸门；没有新授权就不扩大批次。

## 范例

开始撰写逻辑稿前，如果仓库中提供了本地参考范例，读取 [通用逻辑稿结构范例](references/approved-example-template.md)。只模仿它的可读结构、时间戳引用和表达密度，不复用其中的事实、案例、结论或数据。真实课程范例应保存在私有项目中，不要上传到公开仓库。

## 常见危险借口

| 借口 | 新版规则 |
|---|---|
| “我只说在后台运行，没有说已经完成” | 长任务启动前必须进入目标模式；运行中必须明确写“尚未到验收阶段”，并持续监控。 |
| “进程会自己跑，用户下次再问即可” | 完成通知和文件交付是目标的一部分；不得把追问责任转给用户。 |
| “平台没有目标模式，所以普通后台运行也一样” | 启动前告知限制并给可复制启用指令；未经确认不得静默降级。 |
| “Skill 要求只给一个请先看，所以其他文件不用提” | 一个优先入口不等于一项交付；同一条消息仍须列出完整七项真实文件。 |
| “七项文件已经在文件夹里，用户可以自己找” | 逐项给出直接入口和用途；文件夹存在不等于完成交付。 |
| “逻辑稿已经给了，批次报告只是补充” | 报告只能作为后台追溯材料，不能挤占课程内容的阅读入口或验收动作。 |
| “目录名有网盘，所以手机应该能开” | 只依据实际设备验证结果；未经授权不得上传或假定同步。 |

### 红旗：发现即停止当前说法并修正

- 已启动后台任务，却没有活动目标或用户确认的替代方案。
- 准备结束回复，但长任务仍在运行且没有说明后续由谁持续监控。
- 把“已启动”“已有断点”“已完成部分课时”写成阶段完成。
- 准备要求验收，却只附一篇逻辑稿或只给文件夹、报告、`.cmd`。
- 没有实际验证用户当前设备能打开将要发送的文件。

## 边界

- 不许编造课程事实、数字、时间戳、案例、金句、专有名词、因果关系、讲师观点或处理结果。
- 不确定是原话时标注“意译”；无法从来源确认时写入质量问题清单，不得包装成确定结论。
- 不得把“模型整理”冒充“老师原话”；如果模型无法从逐字稿逐字确认引文，必须降级为“未单独提取”“意译”或“模型提炼”。
- 不得把自动生成的“重点核对”理解为用户必须逐条重看；它只是风险排序，人工工作量应集中在少数高风险项。
- 来源缺失、字幕不清、硬字幕未知或时间戳异常时，明确标记“待人工核对”。不要为了文章顺滑擅自补全。
- 拿不准用户意图、批次范围、设备交付方式、云端权限、删除操作或内容判断时，先问用户；未得到回答前保持现状。
- 不得把范例当作模板答案。新课程的结构可以相似，全部内容必须来自新课程来源。
- 不得把“没有自动质量标记”等同于内容完全正确，也不得声称用户已经验收其未明确看过的文件。
- 不得自动删除源视频、扩大到未批准视频、生成整门课程结论、上传网盘、调用远程模型或付费服务。
- 如果本地处理与手机阅读需求冲突，先说明冲突并请求用户选择授权方案，不擅自用任何云盘解决。
