---
name: summarize-video-course
description: "将本地视频课程、字幕或逐字稿整理为可读、可搜索、可追溯的七项知识资料。适用于课程转写与总结、逻辑稿、知识点卡片、案例与金句、已有笔记升级、分批处理、人工验收和本地知识库入库。默认本地处理，不上传课程内容。"
---

# 视频课程总结与交付 v3.3 公共版

本 Skill 用于把课程来源整理成真正可阅读、可检索、可复核的文字知识库。它不包含任何具体课程、个人目录、内部项目状态或私有运行记录。

## 使用前先划清边界

- 默认只在用户本地处理视频、音频、字幕、逐字稿和整理稿。
- 不把课程内容、字幕、转写结果、个人路径、运行日志或提示词发送到远程模型、付费 API 或云端服务，除非用户明确授权并确认版权与隐私风险。
- 源视频只读：不得复制、移动、重命名或删除。临时媒体只写入项目约定的工作目录。
- 正式输出只保留 Markdown 和 JSON，不把视频、音频、切片或转码文件放入最终成果目录。
- 所有索引只记录相对来源路径、时长、校验值和处理标记，不记录绝对本地路径、用户名或设备信息。
- 如果公共仓库用于发布 Skill，只提交通用规则、通用模板和脱敏示例；不得提交任何真实课程资料。

## 按需读取参考文件

- 生成、修复或验收知识点卡片时，先读取 [知识点卡片标准](references/knowledge-card-standard.md)，先建立覆盖清单，再写卡片。
- 撰写逻辑稿时，读取 [逻辑稿可读性闸门](references/logic-note-delivery-gate.md)。
- 需要参考表达结构时，只读取 [逻辑稿通用示例](references/logic-note-example-template.md)，只学习结构和信息密度，不复制其中的占位内容。

## v3.3：课程收尾、全量终检与状态闸门

最后一个已计划课时完成，不等于可以机械地启动“下一批”，也不等于只检查最后一批就能宣布整门课程完成。达到计划总课时后，按以下规则收尾：

1. **先计算剩余课时。** 每次准备启动批处理前，比较“已发布课时数”和“计划总课时数”。两者相等时写明“课程处理阶段已结束，不存在下一批”，并拒绝重复转写或重复整理。
2. **执行全课程终检。** 扫描全部正式课时，而非只检查最后一批：七项资料、逻辑稿截断、学习要点、具名知识点块、卡片可识别性、索引链接、输出媒体文件和绝对路径/个人信息泄露。旧课时有问题时只能写“课程收尾修复中”。
3. **写入唯一状态文件。** 在项目工作目录维护一个稳定的 `run_status.json`，至少记录：`state`、完成数/计划数、当前或最后批次、最后检查时间、下一动作、是否存在下一批。状态可取 `running`、`awaiting_batch_acceptance`、`awaiting_final_acceptance`、`failed`、`course_complete`。回复进度前先读取它。
4. **把最终人工确认作为独立动作。** 最后一批通过自动检查后，状态是 `awaiting_final_acceptance`；用户明确确认后，先跑全课程终检，再写入 `course_complete`。自动检查通过不应冒充用户已阅读；用户已明确确认后，也不得继续称“等待验收”。
5. **提供课程级知识地图。** 在课程根目录生成一份课程知识地图：按原课程章节链接总览、逻辑稿和知识点卡片；它只做导航，不编造跨课结论。

## v3.2 核心硬闸门

### 1. “完成”必须有三种证据

只有同时满足以下条件，才能向用户说“本批完成”：

1. 处理进程已退出，状态文件明确为 `complete`。
2. 预期课时全部发布，并通过独立验证：七项文件齐全、非空、可解析、来源可追溯。
3. 同一条消息已经交付用户能打开的真实可读文件。

缺少任何一项，只能写“处理中”“待修复”或“尚未到验收阶段”。

### 2. 逻辑稿先过人读检查

发布前逐篇打开 `01-逻辑稿.md`，确认：

- 开头有 3–8 条完整的“本课学会什么”，每条都能独立理解并带来源时间戳。
- 有至少两个具名内容块，标题不是孤立编号、单个词、残句或截断标题。
- 每个内容块至少说明一个完整判断、方法、步骤、场景或例子。
- 没有半截时间戳、未闭合引号、未完成句或模型输出截断残留。
- 正文不是“知识点标题 + 同一句复述”，也不是只复制学习要点的空壳。

任一项失败，课时状态为“知识整理待修复”，不得发布或请求验收。

### 3. 首课同类故障熔断

批次首课首次出现结构缺失、输出截断、卡片字段不全、案例误判或解析失败时：

1. 暂停整批继续推进，但保留已有逐字稿和原始模型结果。
2. 只用首课真实来源制作兼容修复样本。
3. 通过七项结构、逻辑稿人读、来源时间戳、无媒体和来源指纹检查后，才恢复本批其余课时。
4. 修复样本失败时，明确写“批次暂停，正在修工具”，不得伪装成处理中或完成。

### 4. 批内技术修复不重复索要确认

同一批次范围内的重试、兼容解析、机械补全、断点续跑、备份、校验和三副本同步属于已授权的技术动作。只有批次验收和下一批启动需要用户决定。

### 5. 修订必须同步并可回退

修订正式课时时：

- 先把旧文件备份到工作目录。
- 同步正式课时、批次记录和断点状态包三处权威副本。
- 逐文件比对三处 SHA-256，确认内容一致。
- 不在此授权范围内修改源视频或原始来源记录。

## 什么时候使用

- 把本地课程视频、字幕或逐字稿整理为总览、逻辑稿、知识点卡片、案例与金句。
- 处理少量样课、固定批次或整门课程。
- 将文字成果放入 Obsidian 或其他本地知识库。
- 升级已有课程笔记结构，但不重新覆盖已经验收的正式成果。
- 修复文件难找、正文难读、来源不可追溯或交付入口不清楚的问题。

纯媒体扫描可以使用专门扫描工具；一旦涉及知识整理、可读性或人工验收，就使用本 Skill。

## 每课必须产出的七项资料

每个课时目录必须包含：

```text
00-本课总览.md
01-逻辑稿.md
02-时间戳逐字稿.md
03-知识点卡片.md
04-案例与金句.md
05-质量问题清单.md
media_index.json
```

- 总览：先写“本课学会什么”，再说明本课解决的问题、核心结论、阅读顺序和适用对象。
- 逻辑稿：按本课实际类型重组正文，保留可核对时间戳。
- 时间戳逐字稿：保存来源记录，不把它当作日常阅读稿。
- 知识点卡片：按真实知识点覆盖生成，不设固定数量上限。
- 案例与金句：只收录来源中真实存在且可定位的案例、原话或明确标注的意译。
- 质量问题清单：记录漏写、歧义、专有名词、时间戳、图示缺失和待人工核对事项。
- `media_index.json`：记录必要的相对来源、时长、校验值、处理状态和七项文件映射。

## 生成前的内容判断

先判断课程属于以下哪一类，再选择结构：

- 框架方法型：重点是分类、模型、公式、方法、适用场景和案例。
- 课堂推导型：按提出问题、解释概念、推导判断、案例或练习推进。
- 清单工具型：重点是类型、模板、步骤、写法、规则和例子。
- 混合型：外层保留授课顺序，内层拆成独立知识点块。

自动判断只是路由提示。复杂课程可以人工指定类型，但必须记录判断依据。

## 阅读层的最低要求

`00-本课总览.md` 和 `01-逻辑稿.md` 开头都必须有 3–8 条带时间戳的“本课学会什么”。之后至少包含：

- 本课解决的问题：用完整句回答课程解决了什么问题。
- 核心结论：列出 2–5 条完整判断句。
- 实际操作顺序：列出 2–6 步，每步以明确动词开头。
- 适合谁看：如实说明对象；来源没有说明时写“逐字稿未明确说明适用对象”。

这些栏目不能使用逐字稿残句、孤立时间戳或“待核对摘录”充数。任何栏目不成句时，课时停在 staging 或“知识整理未完成”。

## 知识点卡片规则

- 生成前先建立“时间段—知识点类型—具体名称—公式或步骤—例子—核对级别”覆盖清单。
- 每种独立类型、模型、公式、步骤、规则、适用场景、正反例和练习分别判断是否成卡。
- 每张卡只表达一个主题；标题具体且不能重复。
- 每张卡必须有主题、具体例子、内容性质、来源时间戳和人工核对级别。
- 能逐字回到来源的短句才标为“老师原话”；模型归纳标为“模型整理”；不确定时标为“意译”或“待人工核对”。
- 只有逐字引用、具体数据、易错术语、公式方向、来源歧义或强因果结论才标“重点核对”。普通归纳标为“常规阅读”。
- 发布前检查标题不重复、字段非空、时间戳不倒退、例子与知识点对应。缺口写入质量清单并停在 staging。

## 案例与金句规则

写“本课未发现”前，必须扫描来源中的“比如、例如、案例、假设、真实产品、用户场景”等提示及其前后文。

有具体情境时，至少整理为：

```markdown
### 案例标题

- 案例类型：真实案例 / 行业案例 / 课堂示例 / 正反例 / 假设案例
- 背景：主体或产品、原状态以及触发问题
- 问题与分析：遇到什么问题，为什么需要改变
- 动作/改造方式：采取了什么动作或方案
- 结果：来源没有说明时写“来源未说明”
- 可复用启发：读者可以迁移的判断或做法
- 来源：时间戳或经确认的外部链接
- 内容性质：老师原话 / 模型整理 / 意译 / 外部资料整理
```

如果材料包含目标、数据、调整、失败或限制，再补充项目背景、目标与初始问题、执行动作、数据结果、关键转折、失败限制和可复用经验。没有来源的数据不得补写。

金句必须是能脱离上下文使用的完整表达，有来源时间戳，并标明“老师原话”“原话候选（待核对）”或“模型提炼”。无法逐字确认时不得加引号。

## 音频、视觉和转写异常

- 无声或纯画面操作视频先分流。若关闭语音活动检测后出现循环、重复或与画面不符的 ASR 幻觉，拒绝该转写，不得为了凑资料发布。
- 确认为无旁白操作录像时，来源模式标记为 `visual_operation_log`，只整理从画面确实观察到的操作顺序。
- 视觉模式不得写“老师说”、老师原话或金句，不得猜测模糊文字、鼠标动作、操作目的或画面外事实。
- 课件图表无法读取时只写“课件图示未提取”；若它可能承载核心结论，必须列入质量清单并暂停正式发布。
- 本地模型失败时保留断点并停止该课，不得用逐字稿摘录占位版冒充正式知识稿。

## 课程归档和批次控制

推荐采用“一门课程一个根文件夹”：

```text
outputs/
└── <课程名>/
    ├── 00-课程总览.md
    ├── 课程级索引与 media_index.json
    ├── 课时资料/
    │   └── <课时名>/七项正式成果
    └── 99-处理记录/扫描、试验、批次和历史记录
```

- 正式成果进入 `课时资料/`；临时稿、模型原始回复和批次报告进入 `99-处理记录/` 或工作目录。
- 一门课程只能有一个权威阅读入口；历史副本只能留在可恢复记录中。
- 每批结束后停在人工验收闸门。未经明确通过，不启动下一批。
- 已验收课时不自动覆盖；已有笔记升级先写入 staging，确认后再发布。

## 长任务和状态表达

预计超过一次回复、运行超过 60 秒、需要轮询或涉及多个课时的任务，都视为长任务。

- 如果平台支持持久目标，先创建目标，再启动处理；目标写清范围、终点、验证和主动交付动作。
- 如果平台不支持持久目标，先告知用户限制，改用可恢复的分段执行；不得把普通后台进程说成持续监控。
- 运行中只能说明当前课时、完成数量、最后检查时间和下一动作。
- 进程存在、断点存在、部分完成或报告生成，都不等于任务完成。
- 完成后主动交付真实可读文件；不要让用户靠追问确认结果。

推荐状态格式：

```text
现在：第 N 批仍在处理中，尚未到验收阶段。
当前：已完成 X/Y，正在处理第 Z 课。
下一步：继续监控、验证并生成完整交付。
用户现在不用操作；完成后会主动发送实际课程文件。
```

## 批次验收交付闸门

请求验收时，同一条消息必须包含：

1. 本批一篇真实存在、已经打开验证的 `01-逻辑稿.md`，作为“请先看”入口。
2. 该课七项资料的逐项入口和用途。
3. 当前设备是否能打开这些入口的说明。
4. 明确说明未确认前不会启动下一批、不会删除源视频、不会扩大处理范围。

不能只给文件夹、脚本、manifest、日志、状态文件或批次汇总。

## 发布前自检

至少检查以下项目：

- 七项资料齐全、非空、名称准确。
- 六个 Markdown 可读，`media_index.json` 能解析。
- 时间戳有序且不超过来源时长。
- 卡片标题不重复、必填字段非空、例子对应知识点。
- 原话、模型整理、意译和重点核对边界明确。
- 案例有来源；没有案例时已完成提示扫描并如实记录。
- 输出目录没有音视频、绝对源路径、用户名、密钥或个人信息。
- 正式课时、处理记录和断点状态包的修订副本一致。
- 将交付给用户的同一文件入口已经现场重新打开验证。

## 公共仓库发布自检

上传到公开 GitHub 仓库前，只保留：

- `SKILL.md`；
- `agents/openai.yaml`；
- `references/` 下的通用标准和通用示例；
- 仓库级 README、许可证和必要的 `.gitignore`。

禁止上传：

- 真实课程视频、音频、字幕、逐字稿、逻辑稿、卡片、案例页和处理日志；
- 课程名称、讲师姓名、学员姓名、客户信息、订单信息和内部项目编号；
- `C:\Users\...`、`/Users/...`、网盘目录、局域网地址、绝对源路径和个人设备信息；
- API key、OAuth token、密码、生产配置、私有仓库地址和私人聊天内容；
- 含真实课程事实的示例文件、截图、manifest、哈希清单或备份目录。

## 执行流程

1. 读取项目规则、状态和实际文件，确认范围、未批准范围和设备。
2. 先做小样，估算本地计算、磁盘和模型资源，再取得批次授权。
3. 识别来源类型：语音逐字稿、字幕、已有转写或视觉操作记录。
4. 先建立知识点覆盖清单，再生成七项资料。
5. 通过结构、内容、来源、隐私和可读性检查。
6. 现场打开最终文件，按验收闸门交付，并停在人工确认处。

## 边界

- 不编造课程事实、数字、时间戳、案例、金句、专有名词、因果关系或讲师观点。
- 不把模型整理冒充老师原话；无法逐字确认的引文降级为意译或模型提炼。
- 不把“没有自动质量标记”等同于内容完全正确。
- 不擅自扩大到未批准的视频，不自动删除或修改源视频。
- 不自动上传课程内容，不调用远程模型或付费服务，不把本地路径当作手机可访问链接。
- 如果用户要求公开发布，先完成内容、隐私、版权和密钥扫描；发现不确定项时停止上传并说明风险。
